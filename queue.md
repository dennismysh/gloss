# Reading Queue

Add URLs below (one per line under **Pending**). When you push to `main`, a GitHub Action will automatically fetch, analyze, and add each article to your reading list.

You can also add URLs from the app using the **+ Add article** button.

## Pending


## Processed
